language: go

services:
  - docker

go:
  - '1.12.6'

env:
  - GO111MODULE=on

before_install:
  - go get -u golang.org/x/lint/golint

install:
  - go mod vendor
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

script:
  - make lint
  - make test

deploy:
  provider: script
  skip_cleanup: true
  script:
    - make image
    - make publish
  on:
    tags: true
